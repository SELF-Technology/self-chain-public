rootProject.name = 'self-chain'

// Include the private repository as a composite build
includeBuild '../private-repo'

// Configure composite build settings
gradle.beforeSettings { settings ->
    // Configure the private repository location
    settings.includeBuild('../private-repo') {
        name = 'self-chain-private'
    }
}
